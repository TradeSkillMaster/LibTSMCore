{
  "name": "libtsmcore-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:generate": "wowlua_ls doc .. --out-dir api --class LibTSMCore --class LibTSMComponent --class LibTSMModule",
    "docs:dev": "npm run docs:generate && vitepress dev .",
    "docs:build": "npm run docs:generate && vitepress build ."
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  }
}
